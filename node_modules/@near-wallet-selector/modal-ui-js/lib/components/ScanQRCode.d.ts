import type { ModuleState, Wallet } from "@near-wallet-selector/core";
export declare function renderScanQRCode(module: ModuleState<Wallet>, params: {
    uri: string;
    handleOpenDefaultModal: () => void;
}): Promise<void>;
